# /internal

This directory is for private application and library code that is not intended for external consumption. Go's package visibility rules prevent other modules from importing packages within an internal directory, ensuring internal code remains encapsulated.